use crate::prelude;

#[test]
fn generate_v5_uuid() {
    let name = "Duncan"; 

//    assert_eq!(uuid.get_variant(), Some(Variant::RFC4122));
 //   assert_eq!(uuid.get_version(), Some(Version::Sha1));
}


